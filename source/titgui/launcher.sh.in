#!/usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE}")" && pwd)"
exec "$SCRIPT_DIR/@REL_PATH_TO_EXE@" "$@"
echo "Failed to launch the application. Please check that it is installed correctly." >&2
exit 1
