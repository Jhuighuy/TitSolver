# `tit/par`

This library contains the everything related to parallelization.
It is mostly a wrapper for Intel's amazing Thread Building Blocks framework with
some additions that are needed make using it easier and more C++ friendly.

Everything in this library should be placed into `tit::par` namespace.
