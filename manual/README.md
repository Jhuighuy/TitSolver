# `manual`

This folder contains the Astro source code for the solver's user manual.

The documentation is built as a fully static site and installed into the
`manual/` directory so the help browser can open it directly from the local
filesystem.
